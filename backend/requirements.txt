# Core API
fastapi>=0.115
uvicorn[standard]>=0.30
sqlmodel>=0.0.22
python-multipart>=0.0.9
pyyaml>=6.0
httpx>=0.27

# Backend AI (B6 estimator, B10/B11 RAG)
google-genai>=2.25      # LLM (default provider: Gemini free tier) for briefing (E15) + assistant (E23)
anthropic>=1.8          # alternative LLM provider (LLM_PROVIDER=anthropic)
xgboost>=2.1
scikit-learn>=1.5
chromadb>=0.5
sentence-transformers>=3.0

# Tests
pytest>=8.0
