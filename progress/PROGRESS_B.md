# Progress — Person B (Infra + Backend AI)

> Only Person B edits this file. Person A reads it.
> Status: ⬜ todo · 🟨 doing · ✅ done (matches CONTRACT, tested) · 🟦 done on mock/stub only · ⛔ blocked · ➡️ moved
> B owns **all backend** (incl. Training Hub content + endpoints). Since v2.1 **all Flutter work moved to A** (DECISIONS #14) — B has no Flutter toolchain.

**Last updated:** 2026-09-23 (ownership re-split v2.1)  **Current milestone:** M3 (backend side done)  **Contract version in use:** v2.1

## Tasks

| ID | Task | Endpoints / provides | Milestone | Status | Notes |
|---|---|---|---|---|---|
| B0 | Scaffold: FastAPI skeleton + `/health`, `db.py` (`get_db`), `models.py` (incl. `JobLog`), config, all routers stubbed returning §4 examples | E1 | M0 | ✅ | Do first. Flutter half (`flutter create`, pubspec, `main.dart`, `core/config.dart`) ➡️ moved to A |
| B1 | Seed: 4 machines, ~5 operators, 2 projects, ~8 jobs, ~60 `JobLog` rows (varied weather/experience/actual-vs-planned) | — | M1 | ✅ | B6 depends on this |
| B2 | Core endpoints: login, operators, machines, jobs, assignments | E2–E7, E9 | M1 | ✅ | |
| B3 | Fatigue / rest service (feeds E4, gate at E10) | in E4, E10 | M1 | ✅ | |
| B4 | Flutter data layer: models (incl. `AlertCreate`, `Telemetry`), `ApiClient`, `MockApiClient` + fixtures, `telemetryStreamProvider` (fake 1 Hz telemetry), providers | §6.1 | M1 | ➡️ | Moved to A (v2.1) |
| B5 | `HttpApiClient` for all endpoints (incl. binary TTS, alert POST) | §6.1 | M2 | ➡️ | Moved to A (v2.1) |
| B6 | **XGBoost estimator**: train on `JobLog`, fallback formula, E8 + `train_estimator` script | E8 | M2 | ✅ | Needs B1 seed |
| B7 | Sessions + checklist status + critical-defect gate + briefing (LLM + template fallback) | E10–E17 | M3 | ✅ | Uses B10 checklist content |
| B8 | Simulator engine + scenarios + WebSocket **telemetry stream** (no server-side alerts) | W1, E21, E22 | M3 | ✅ | `POST /sim/scenario` steers values so A's rules fire |
| B9 | Alert storage + list + ack (receives A's on-device alerts) | E18, E19, E20 | M3 | ✅ | Incident log |
| B10 | **RAG**: ChromaDB vector store + `sentence-transformers`; ingest manuals + checklist content; serve checklist (E12) | E12 | M3 | ✅ | Checklists structured; RAG for Q&A |
| B11 | **Q&A assistant** (E23) via RAG + LLM, answers in `lang`, returns sources | E23 | M4 | ✅ | |
| B12 | Sarvam proxy: TTS (E24) + translate (E25); 503 on missing key/timeout, never crash | E24, E25 | M4 | ⏸️ | Deferred (human's call, 2026-09-24): no paid keys; tablet uses on-device `flutter_tts`. E24 already returns 503 (app falls back), E25 echoes. Build later (check docs.sarvam.ai first); IndicTrans2 could take over E25 | Check docs.sarvam.ai for current models |
| B13 | Training content YAML (4 types × 3 levels, start with excavator) + endpoints | E26, E27 | M4 | ✅ | |
| B14 | Training Hub screens: steps, video links, quiz, complete, 3 languages (`training_strings.dart`) | E26, E27 | M4 | ➡️ | Moved to A (v2.1). B keeps B13 (content + E26/E27) |
| B15 | Backend tests (`tests/`) + SETUP Backend/AI sections + lead fresh-clone check | — | M5 | 🟨 | Backend part done: 215 tests, SETUP §3/§5/§7 current, backend fresh-clone check passed (SETUP §8). Remaining at M5: re-run after final commits + joint run with A's app |

## Ready for A
- **B0 backend skeleton:** every endpoint E1–E27 + W1 exists and returns the CONTRACT §4 example JSON (stubs). Run it (SETUP §3) and point the app's real client at it; browse/try everything at `http://localhost:8000/docs`. W1 streams the §5 example telemetry every second for any session id. Errors follow §1 (`{"error": {"code", "message"}}`); bad enum values → 422 `VALIDATION_ERROR`. E24 `/voice/tts` returns 503 `UPSTREAM_UNAVAILABLE` until B12 — use on-device `flutter_tts`.
- **B1 seed data** (`python -m app.seed`): IDs `op_001`–`op_005`, `mc_001`–`mc_004` (one per machine type), `prj_001`/`prj_002`, `job_001`–`job_008`, `asg_001`–`asg_008`. Contract example IDs (`op_001` Ravi / `mc_001` / `job_001` / `prj_001`) are real seeded rows, so your mock fixtures line up. Demo logins + PINs + rest-status story are in SETUP §3.2 (`op_001` / 1234 = Ravi, ta-IN). Served live since B2 (below).
- **B2 live on seeded data: E2–E7, E9.** Stubs remain only for voice (B12: E24 TTS → 503, E25 echoes). Notes for your `HttpApiClient`:
  - **B3 live:** `hours_today` = hours in the *current shift* (work with breaks < 10 h counts as one shift; resets to 0 after 10 h rest, so night shifts crossing midnight count correctly). `hours_7d` = rolling 7 days. `rest.status`: `must_rest` at 12 h shift or 60 h/7 d (with `next_allowed_start` + `reason`), `warning` at ≥ 9.6 h shift or ≥ 51 h/7 d (with `reason`), else `ok` (nulls).
  - **E10 gate:** `POST /sessions` for a `must_rest` operator → `409 REST_REQUIRED`, body `{"error": {"code", "message" (= reason), "next_allowed_start": "…Z"}}`. Demo: `op_004` Mohit is always blocked; `op_002` Priya shows `warning` but may start. Rest of E10 (persisting the session) is still a stub until B7.
  - E5 `range`: `day` = today (IST), `week` = today + next 6 days, `month` = today + next 29 days; sorted by date then shift.
  - Error codes (all in the §1 format): E2 `401 BAD_PIN`, `404 OPERATOR_NOT_FOUND`; E4/E5 `404 OPERATOR_NOT_FOUND`; E6 `404 MACHINE_NOT_FOUND`; E7/E8 `404 JOB_NOT_FOUND`; E9 also `422 MACHINE_TYPE_MISMATCH`, `409 ASSIGNMENT_CONFLICT`; bad query/body → `422 VALIDATION_ERROR`.
  - Times are ISO 8601 UTC with `Z` (e.g. `scheduled_start: "…T03:30:00Z"` = 09:00 IST).
  - The server auto-seeds an empty DB on startup; `python -m app.seed` still re-seeds on demand.
- **B6 live: E8 `/jobs/{id}/estimate` is real XGBoost.** Uses the job's weather + the assigned operator's experience on that machine type. `effect_pct` is a signed whole-number %: `weather` vs clear weather, `operator_experience` vs an intermediate operator. `range_hours` = p10–p90 from cross-validated residuals. `project_completion_date` = latest estimated finish of the project's open jobs (IST date). Examples on seed: `job_001` ≈ 6.7 h [6.1, 7.2] (rain +20 %, expert −9 %); `job_008` ≈ 10.7 h (rain, novice). Falls back to a formula (same shape) if the model is missing — never errors.
- **B7 live: E10–E17** (real sessions, checklist, briefing). Flow `pre_start → (E14) briefing → (E16) active → (E17) ended`; a call in the wrong state → `409 INVALID_STATE` (`state`, `expected`).
  - E10 errors: `404 *_NOT_FOUND`, `409 REST_REQUIRED`, `422 MACHINE_TYPE_MISMATCH`, `409 MACHINE_UNAVAILABLE` (maintenance), `409 MACHINE_IN_USE` / `SESSION_ALREADY_ACTIVE` (both with `session_id`). Creating a new session abandons the operator's unstarted ones, so the demo can restart freely.
  - E12 checklist: 17–18 items per machine type in 4 sections (Walk-around, Fluids, Cab and controls, Safety devices); excavator `chk_01`/`chk_02` = contract example. Item text is English.
  - E13 → `404 CHECKLIST_ITEM_NOT_FOUND`; locked (`409 INVALID_STATE`) after complete.
  - E14 → `422 CRITICAL_DEFECT` (`items`) checked first, then `422 CHECKLIST_INCOMPLETE` (`items` = still pending). Non-critical `defect` and `na` are allowed. Demo: mark `chk_02` defect → blocked → set `ok` → complete.
  - E15 briefing only in `briefing`/`active`. Without an LLM key it's a template: fixed phrases in en/hi/ta, job titles/site hazards stay English. With a key the LLM writes it fully in `lang`. `estimated_hours` always from the XGBoost estimator.
  - E16 sets machine `in_use`, job `in_progress`; E17 frees the machine and returns the summary (`alerts_*` count stored alerts once B9 lands; `idle_minutes` = 0 until B8).
- **B8 live: W1 + E21/E22.** Connect `ws://<host>:8000/ws/sessions/{id}` once the session is `active` (after E16): one §5 `telemetry` frame per second (the latest frame is sent immediately on connect). Not active / unknown → `{"type":"error","code":"SESSION_NOT_ACTIVE"}` then close; after E17 connected clients get the same and are closed.
  - Normal telemetry per machine type (dig / load-carry / drill / haul cycles) **never trips your rules** (tested over 10 simulated min each).
  - `POST /sim/scenario {session_id, event}` → 202 (`409 SESSION_NOT_ACTIVE`, `404 SESSION_NOT_FOUND`, `422 UNKNOWN_EVENT`). Each nudge lasts ~15 s then returns to normal; a new nudge replaces the running one. Targets sit just past your `TelemetryThresholds`: `seatbelt_off` → false; `proximity` ramps 9→1.5 m (warning then critical); `overheat` 88→108 °C (warning then critical); `overload` 85→104 %; `unsafe_operation` 18 km/h @ 85 %; `excessive_idle` → idle_seconds 170→185 (fires ~10 s in). Camera events (`drowsiness` …) are accepted as no-ops with a note.
  - E17 summary `idle_minutes` is now real (simulated idle time).
- **LLM default is now Gemini (free tier)** — briefing text becomes fully Tamil/Hindi once B's key is in `.env`; template fallback otherwise. Speech stays on your side (on-device STT/TTS).
- **B9 live: E18–E20 incident log.** `POST /sessions/{id}/alerts` with your `AlertCreate` (your Dart `toIso8601String()` timestamps are fine; `ts` optional) → 201 stored `Alert` with a global `alr_NNN` id. Accepted while the session is `active` (and `ended`, so an alert in flight at end isn't lost); `pre_start`/`briefing` → `409 SESSION_NOT_ACTIVE`. A retried post (same type + severity + ts) returns the existing alert with 200 — safe to retry. An escalation (warning → critical) is a new alert. `GET …/alerts` = newest first. `POST /alerts/{id}/ack` is idempotent; unknown → `404 ALERT_NOT_FOUND`. E17 summary `alerts_total` / `alerts_critical` now count these.
- **M3 backend is complete:** start session → checklist → briefing → live telemetry → scenario → your on-device alert → E18 → ack → end with summary, all real.
- **B11 live: E23 `/assistant/ask`.** `{machine_id, session_id?, question, lang}` → `{answer, lang, sources[{doc, section}]}`. Hindi/Tamil question → translated to English → top-3 sections of that machine's manual → Gemini answers **in `lang`** (1–4 short sentences, read-aloud friendly, numbers kept) citing the sections it used. Off-topic → "couldn't find that in the manual" in `lang`, `sources: []`. **Use the returned `lang` for TTS**: with no LLM available the answer is the English manual text and `lang` is `en-IN`. Measured live: median ~3 s, worst ~8 s. Errors: `404 MACHINE_NOT_FOUND` / `SESSION_NOT_FOUND`, `422 EMPTY_QUESTION`.
- **B13 live: E26/E27 Training Hub** (for your A18). `GET /operators/{id}/training/next?machine_type=` → the module for the operator's experience on that machine type (novice if none): 12 modules, 4 machines × novice/intermediate/expert, 30–45 min, 3–8 steps (`kind` text|tip, `url` null — no video links yet) + 3–4 quiz questions; content matches the manuals. `POST /training/{module_id}/complete {operator_id, score}` → `{"ok": true}`; **`score` = number of correct answers (0 … len(quiz))**, out of range → `422 SCORE_OUT_OF_RANGE`; unknown module → `404 TRAINING_NOT_FOUND`. Content is English (translation: later stage). Demo: Arjun `op_003` → `trn_ex_novice_01`.
- **All placeholder data is gone** except E24/E25 (Sarvam, optional B12).

## Blockers
_None_

## Replies to A's requests (PROGRESS_A, 2026-09-24)
1. **Assistant gives no answer in hi/ta (E23)** — the backend *does* translate hi/ta → English → retrieve → answer in `lang` (DECISIONS #17); verified live with keys: 7/7 Hindi questions (Devanagari, mixed, romanized) answered correctly with sources. "No relevant info / contact supervisor" for **every** hi/ta question means the **backend your app talks to can't reach an LLM** (no `LLM_API_KEY`/`GROQ_API_KEY` in *its* `.env`, or Gemini down with no Groq fallback), so the question can't be translated. With the latest backend: that server's first console line shows `LLM: gemini ✓/✗ … → groq ✓/✗`, and the reply now says **"language service unavailable — ask in English or try again"** instead of blaming the manual. Fix = add the keys to that server's `.env` + restart (or point `API_BASE` at B's laptop).
2. **Training text not localized (E26)** — ✅ done in v2.3: E26 and the new E29 honour `Accept-Language`/`?lang=`; all 12 modules have committed hi/ta translations (title, steps, quiz, explanations). Better: build the Hub on E28/E29/E30 (P1 item 1 below).
3. **Training video URL** — there are **no video steps**: video lessons are paused (DECISIONS #22), every step is `kind` `text`/`tip` with `url: null`. Hide video UI. (If it ever returns, URLs will be relative to `API_BASE`, e.g. `/media/…`.)
4. **Your client-side 409 resume** — 👍. One caveat: when you resume an existing session, route by its `state` (`GET /sessions/{id}`): `pre_start` → checklist, `briefing` → briefing, `active` → **live** screen. Sending an `active` session back to the checklist gives `409 INVALID_STATE`.

## Requests for partner (A)
_Consolidated 2026-09-24 after CONTRACT v2.2 + v2.3 — audited against `origin/main` @ b785d81. Priority order = demo impact. Backend for all of these is live and tested (249 tests)._

### ✅ Already done on your side (no action)
- Android `INTERNET` + `RECORD_AUDIO` permissions and `usesCleartextTraffic="true"`.
- Assistant speaks the answer with the **returned** `ans.lang` (correct — with no LLM the answer is English and `lang` = `en-IN`).
- TTS uses on-device `flutter_tts` only, so the backend's `503` on `/voice/tts` never matters.
- Old requests (pubspec block, models barrel, `TrainingHubScreen` ctor, `.gitignore`) — closed.

### 🔴 P1 — needed for the demo
1. **Training Hub screens (your A18) on the new API (CONTRACT v2.3).** Today `training_hub_screen.dart` is a 24-line placeholder. Suggested flow — 3 screens:
   - **Hub home** ← `GET /operators/{operatorId}/training/plan` (`TrainingPlan`). Show `progress` on top (🔥 `streak_days`, `modules_passed`, `avg_score_pct`, `total_minutes` to do), then `items` in `priority` order: card = `title`, `duration_min`, level/machine chip, `status` (`todo`/`done` ✓), `last_result` ("last 2/4"), and **every `reasons[].text` as a line/chip** — these are the selling point ("2 seatbelt alert(s) in the last 7 days", "Hazard on your job: …", "Last score 2/4: retake to pass"). Texts are already localized — don't translate them in ARB. `reasons[].code` (`recent_alert` / `job_hazard` / `retake` / `assigned` / `new_machine` / `level_up` / `keep_fresh`) is for icons/colours.
   - **Module** ← `GET /training/modules/{module_id}` (`TrainingModule`, same shape as E26): steps (`kind` `text` / `tip`; `url` is always null for now — no video), then the quiz (`q`, `options`). **Don't grade on the device.**
   - **Result** ← `POST /training/{module_id}/submit` body `{"operator_id": "...", "answers": [1, 2, 0, null]}` (one entry per question, `null` = skipped) → `QuizResult`: `score`/`total`/`passed` (≥ 70 %) + per question `correct`, `chosen`, `correct_index`, `explanation`, `source` `{doc, section}`. Show ✅/❌ per question, the explanation and "Manual § {section}". Then **refetch the plan** — the module turns `done`, "retake" disappears, streak/attempts update.
   - New Dart models: `TrainingPlan`, `PlanItem`, `PlanReason`, `LastResult`, `TrainingProgress`, `QuizResult`, `QuestionResult` (+ reuse `AnswerSource` for `source`); `ApiClient`: `trainingPlan(operatorId)`, `trainingModule(moduleId)`, `submitQuiz(moduleId, operatorId, answers)`; mock versions for `USE_MOCK=true` (copy the JSON examples in CONTRACT §4 "TrainingPlan / QuizSubmit / QuizResult").
   - Route: the Hub now only needs `operatorId` (the plan picks machines itself); `machineType` can stay optional.
   - Errors: `422 ANSWER_COUNT_MISMATCH` / `ANSWER_OUT_OF_RANGE`, `404 TRAINING_NOT_FOUND` / `OPERATOR_NOT_FOUND`.
   - Demo data (after `python -m app.seed`): **Ravi `op_001`** → 3 items with alert/hazard/retake reasons, 3-day streak; Priya `op_002` → 1 item, 1-day streak; Arjun `op_003` → "keep fresh" items (assign him a loader job via Swagger E9 to see "You're new to the wheel loader").
2. ✅ **Send the display language (CONTRACT v2.2)** — done on your side (Accept-Language on every request, per your note). Kept for reference: `HttpApiClient` sets no language header today, so names, job titles, checklist items, estimate notes, **training plan reasons and module content** all come back English. Fix once: `Accept-Language: <contractLang(locale)>` (`en-IN` / `hi-IN` / `ta-IN`) in `BaseOptions.headers`, updated on language switch (or a dio interceptor reading `localeProvider`). After a switch, refetch what's on screen. `?lang=` also works per call; the briefing already passes it.
3. **Real telemetry WebSocket (your A17).** `ws_client.dart` still always returns `mockTelemetryStream()`, so with `USE_MOCK=false` the gauges never show the backend simulator and `POST /sim/scenario` never reaches the tablet (demo beat 7). Connect `web_socket_channel` to `ws://<API_BASE host>/ws/sessions/{id}` once the session is `active` (after E16): one CONTRACT §5 `telemetry` frame per second; `{"type":"error","code":"SESSION_NOT_ACTIVE"}` then close = session ended/not started. Scenario list + expected rule behaviour: SETUP §7.
4. **Handle backend rejections (4xx) in the UI.** No `DioException` handling today (`job_screen.dart` → `createSession`, `pre_start_screen.dart` → `completeChecklist`, and the new quiz submit). The code is in `e.response?.data['error']['code']`, message in `['message']`:
   - `409 REST_REQUIRED` (Mohit) → show `message` + `next_allowed_start`.
   - `422 CRITICAL_DEFECT` → highlight `items` (ids of critical defects); `422 CHECKLIST_INCOMPLETE` → `items` still pending.
   - `409 SESSION_ALREADY_ACTIVE` / `MACHINE_IN_USE` → include `session_id` (offer "resume/end that session").
   - `409 INVALID_STATE` → `state` + `expected` (step out of order).
   (The 409 you're investigating now is one of these — full table in SETUP §4.6.)

### 🟡 P2 — polish
5. **Job screen machine choice:** it picks `machines.firstWhere(type == job.machineType)`; the assignment (E5) already carries the right `machine` — pass that through to `createSession`.
6. **Camera permission:** manifest has no `android.permission.CAMERA` — confirm the `camera` plugin merges it (check the merged manifest), else add it; the ML Kit CV demo needs it.
7. **Assistant:** if the answer says the language service is unavailable, the backend your app points at has no LLM key — its first console line shows `LLM: … ✗ NO KEY`; add `LLM_API_KEY` / `GROQ_API_KEY` to that server's `.env`.

### Before every demo run
`cd backend && python -m app.seed` (resets data + the demo story: Ravi's alerts, quiz history, Mohit must rest), start the server with `--host 0.0.0.0`, check its first log line says `LLM: gemini ✓ … → groq ✓`, run the app with `USE_MOCK=false API_BASE=http://<laptop-ip>:8000` (SETUP §3.5 / §4.3).

## Session log
| When | Did | Next |
|---|---|---|
| 2026-09-23 | Created `CLAUDE.local.md` (Person B). Re-split ownership v2.1: whole Flutter app → A (CLAUDE.md §2–4/§9, CONTRACT v2.1, app/CLAUDE.md, backend/CLAUDE.md, SETUP §4, DECISIONS #14). | Get A's OK, then B0 (backend scaffold). |
| 2026-09-23 | B0 ✅: FastAPI app (`main.py`, `config.py`, `db.py`, `models.py` incl. `JobLog`/`Alert`/`ChecklistItemState`, `schemas.py` = §2/§4, `errors.py` = §1), 10 routers + `ai/router_assistant.py` + `ai/router_voice.py` all stubbed with §4 data, W1 stub stream. `tests/test_b0_stubs.py` 15/15 pass; uvicorn boots, `/docs` lists all 26 contract paths + `/health`. `.gitignore` for estimator/chroma. SETUP §3 → python3.11. | B1 seed, then B2 core endpoints + B3 fatigue. |
| 2026-09-23 | B1 ✅: `app/seed.py` — 5 operators (en/hi/ta, fatigue cast: ok / warning 52 h / must_rest after 12 h night), 4 machines, 2 projects, 8 jobs (with weather + hazards), 8 assignments across day/week/month, 23 past ended sessions as work history, 60 `JobLog` rows with a real weather/experience/machine signal. All relative to seed time (IST day). Switched DB convention to tz-aware UTC (SQLModel requires it). `tests/test_seed.py` incl. a 24-hour sweep for overlap/future-work; 48/48 pass. SETUP §3.2 demo logins. | B2 core endpoints on seeded data, then B3 fatigue. |
| 2026-09-23 | B2 ✅: E2 login (401 `BAD_PIN`), E3/E4 operators, E5 assignments (day/week/month from IST today), E6 machines, E7 job, E9 create assignment (type-mismatch 422, operator/machine double-booking 409, `asg_NNN` ids). Shared `views.py` (row→§4 builders, 404 codes), `clock.py` (IST today), `ids.py`, `services/fatigue.py` placeholder for B3. Schemas `from_attributes`. Auto-seed empty DB on startup. `tests/test_core.py`; 58/58 pass; live smoke on a fresh DB OK. | B3 fatigue service (hours + rest + E10 gate). |
| 2026-09-24 | B3 ✅: `services/fatigue.py` — shift grouping (breaks < MIN_REST merge), current-shift hours, rolling 7-day hours, must_rest (shift cap → +10 h; weekly cap → when window clears) / warning (80% shift, 85% week) with reasons. E4 uses it; E10 now returns 409 `REST_REQUIRED` (+ `next_allowed_start`). Seed fix: Ravi's yesterday off so his today-shift never merges with yesterday when seeded at night. `tests/test_fatigue.py` incl. demo-story check at all 24 IST hours; 91/91 pass; live check OK. | B6 XGBoost estimator (E8) — or B7 sessions if A needs the mounted flow first. |
| 2026-09-24 | B6 ✅: `ai/estimator.py` (XGBoost predicts actual/planned ratio from machine/weather/experience; counterfactual factors; p10–p90 CV residual band; formula fallback; mtime-cached loader) + `ai/train_estimator.py` (5-fold CV: MAE 0.38 h vs 1.40 h planned-hours baseline on synthetic seed). E8 real incl. project completion date. Server auto-trains if model missing. `ESTIMATOR_PATH` setting. `tests/test_estimator.py`; 99/99 pass; fresh-clone check OK. | B7 sessions + checklist + briefing (M3), B10 checklist content alongside. |
| 2026-09-24 | B7 ✅ + B10 checklist content: `services/sessions.py` state machine (create checks, abandon unstarted, checklist merge/upsert, CRITICAL_DEFECT / CHECKLIST_INCOMPLETE gate, start/end + machine/job status), `data/checklists/*.yaml` ×4 (MSHA 56.14100 / ISO 20474-based), `ai/checklists.py`, `ai/briefing.py` (facts → LLM JSON in `lang`, en/hi/ta template fallback, cached), `ai/llm.py` (Anthropic SDK, structured output, refusal/fallbacks; any failure → `LLMUnavailable`), `services/estimates.py`. Live run caught a 500 when no API key (SDK raises TypeError) → fixed + regression test. `tests/test_sessions.py`; 123/123 pass. | B8 simulator + WS, B9 alerts storage (M3). B10 rest = manuals + Chroma (with B11). Ask user which LLM key they have. |
| 2026-09-24 | B8 ✅: `simulator/engine.py` (MachineSim per machine type, 1 tick = 1 simulated s, idle tracking; SimulatorEngine async loop + subscriber queues, restore active sessions on startup), `simulator/scenarios.py` (overlays tuned to A's thresholds, ramps for warning→critical), real W1 + E22; E16/E17 start/stop the sim, summary idle_minutes real. Gemini provider added to `llm.py` (google-genai `generate_content`, JSON schema output, 15 s timeout; verified API + model names against current Google docs; default `gemini-3.8-flash`, free tier). `tests/test_simulator.py` + `tests/test_llm.py`; 149/149 pass; live 1 Hz WS check OK. | B9 alert storage (E18–E20), then B10/B11 RAG + assistant on Gemini. |
| 2026-09-24 | B9 ✅: real E18 (state rule active/ended, naive ts → UTC, idempotent retry on type+severity+ts), E19 newest-first, E20 idempotent ack; summary counts real alerts. Removed alert stub. `tests/test_alerts.py`; 158/158 pass. M3 backend complete. | B10 manuals + Chroma, B11 assistant on Gemini (M4). |
| 2026-09-24 | SETUP: §3.5 serve the tablet (host/IP/firewall, real-vs-placeholder table), §7 full scenario table + stream watcher, §4.6 error rows; **§4 edited with human's OK** (devices, run, APK build). Requests-for-A rewritten with integration findings from reading origin/main. | B10 manuals + Chroma. |
| 2026-09-24 | B10 ✅: 4 synthetic manuals `data/manuals/*_manual.md` (75 numbered sections, generic/no OEM text, thresholds match simulator + A's rules, excavator §4.2 Operating modes = contract source example); `ai/rag.py` (### chunking, all-MiniLM-L6-v2 English embeddings per human's call → DECISIONS #17, persistent Chroma cosine store with content-hash rebuild, machine filter, RELEVANCE_MIN 0.30 measured: on-topic ≥0.35 / off-topic ≤0.25, background warm-up at startup). English retrieval top-1 11/12 (miss at rank 2). Config default LLM_PROVIDER → gemini. `tests/test_rag.py`; 176/176 pass; live: server up in 0.6 s, store built in background. | B11 assistant (translate → retrieve → Gemini answer in lang → sources). |
| 2026-09-24 | B11 ✅: `ai/assistant.py` (translate→retrieve→answer in lang with used-section sources; not-found in en/hi/ta; no-LLM → English section with lang en-IN), real E23. Live with Gemini: correct grounded ta/hi/en answers. Found + fixed: 3.8-flash often 503 → flash-lite primary; google-genai default 5 retries w/ backoff caused ~2 min calls → attempts=1, 10 s/attempt → median 2.9 s, max 7.7 s. `tests/test_assistant.py` (+LLM fallback tests); 189/189 pass. | Optional Groq fallback if human adds GROQ_API_KEY; B13 training content; B12 Sarvam optional. |
| 2026-09-24 | Groq fallback in `ai/llm.py` (provider chain primary → Groq; strict json_schema; low reasoning; 10 s; no SDK retries; errors prefixed per provider) + `GROQ_API_KEY`/`GROQ_MODEL` config. Live: correct ta/hi answers; calls ~0.6–0.9 s (an earlier ~21 s/call spell was transient network slowness, not reproducible). Noted: small model sometimes *answers* instead of translating → human proposed IndicTrans2 for translation. 197/197 pass. | Decide IndicTrans2 (local translation) with human. |
| 2026-09-24 | **Deferred (human's call): IndicTrans2** (AI4Bharat, local en↔hi/ta translation) to replace LLM translation in the assistant (question → English, English answer → lang) and translate briefing data strings; would also enable a no-key Tamil/Hindi mode. Revisit at a later stage; plan: feasibility check (distilled 200M models, CPU latency, HF terms, machinery-term quality) first. | Continue as planned: B13. |
| 2026-09-24 | B13 ✅: 12 training modules `data/training/*.yaml` (content grounded in the manuals, validated answer_index), `services/training.py`, real E26 (level from operator experience, novice default) + E27 (records TrainingCompletion; score = correct answers, range-checked). Removed `stub_data.py` (nothing left using it). `tests/test_training.py`; 215/215 pass. | B15 (tests/SETUP/fresh-clone) or optional B12 Sarvam. |
| 2026-09-24 | B12 deferred (human's call). Starting B15: fresh-clone check. | |
| 2026-09-24 | B15 (backend part): fresh-clone check on an exact copy of the repo file set (git tracked + new, minus ignored/deleted): SETUP §2–3 literally → pip install OK (~9 min), seed/train OK, pytest 215/215, server + 20-step no-key demo flow all PASS, 0 server errors. Fixed stale SETUP §5.1/§5.3 (assistant no longer 503s; TTS 503 expected), install-time note, §8 checklist. | M5: re-run after final commits + with A's app. |
| 2026-09-24 | Video-lesson prototype `ai/video_lesson.py` (human: good enough; generated in the cloud in the real product, so no offline tests): context from today's job + RAG → LLM storyboard with number-grounding guard → edge-tts narration → Pillow slides + libass-shaped Tamil text → ffmpeg MP4 (+SRT/JSON); bundled OFL Noto fonts; 47 s Tamil lesson for Ravi rendered in 29 s. Hindi 'not available' report: reproduced assistant path works (7/7 Hindi incl. romanized) → cause is a server without LLM access; fixed the misleading reply (now 'language service unavailable'), added startup `LLM: …` status + visible `[app.*]` logs. 216/216 pass. | Human to check which server the app uses (API_BASE) + its console status line. |
| 2026-09-24 | Multilingual display data: `data/i18n/{hi-IN,ta-IN}.yaml` (operators, machines, jobs incl. hazards, all 70 checklist items + sections, estimate terms) + `app/i18n.py` (`?lang=` / `Accept-Language`), wired into E2–E9, E12, E13 and the briefing template (now fully localized without an LLM). CONTRACT v2.2 (additive), DECISIONS #21, request 0 for A. `tests/test_i18n.py` (coverage + script + endpoints); 234/234 pass. | A: send Accept-Language. Next: training content translation. |
| 2026-09-24 | Training Hub v2.3: modules got topics + per-question explanation/manual source; `services/training.py` plan (assignments/hazards/alerts/new machine/retake/level up, localized reasons, progress + streak) + grading; E28 plan, E29 module, E30 submit, E26 ?lang; seed adds Ravi's 3 alerts + quiz history. 24 hi/ta module translations generated once (`services/training_i18n.py`, number + script guards caught 1 structure error, 1 English and 2 romanized outputs → regenerated). Video paused (DECISIONS #22). CONTRACT v2.3. `tests/test_training_hub.py`; 249/249 pass. Also: my plan-preview script reseeded the dev DB by mistake (disclosed; guarded since). | A18 screens on E28–E30. |
